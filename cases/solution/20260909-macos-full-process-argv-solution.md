# 2026-09-09：独立读取完整进程参数与运行时间

## 修复机制

1. 使用 `ps -ww ... -o command=` 单独读取不截断的完整进程参数。
2. 使用第二次 `ps ... -o etime=` 独立读取运行时间，避免两个输出列互相挤占宽度。
3. OpenCode 参数含 `-s/--session` 时继续以该明确 ID 为最高优先级；只有没有 ID 时
   才采用受进程启动时间约束的目录匹配。

该修复不修改 tunnel 绑定或 OpenCode 数据，只恢复对真实运行进程身份的准确读取。

